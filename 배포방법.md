# 퍼머컬처ON 통합 앱 — 배포 방법

## 📁 폴더 구성
```
permaculture-on/
├── index.html        ← 앱 화면 전체 (설정·신고·차단 포함)
├── app.js            ← 앱 동작 (건드릴 일 거의 없음)
├── privacy.html      ← 🆕 개인정보처리방침 (스토어 제출 필수)
├── terms.html        ← 🆕 이용약관 (스토어 제출 필수)
├── data/
│   ├── plantdata.js  ← 동반작물 데이터 (기존 Firebase 데이터 내장)
│   └── content.js    ← ⭐ 영상·농가·이벤트 (자주 수정하는 파일!)
├── manifest.json     ← 앱 아이콘·이름 설정
├── sw.js             ← 오프라인 동작 (수정 시 VERSION 숫자 올리기)
├── icons/            ← 앱 아이콘
└── textures/         ← 종이·흙·나무 질감 이미지
```

⚠️ **이 폴더가 유일한 원본입니다.** 예전에는 index.html이 Downloads 바로 아래에 따로 있었는데,
이제 이 폴더 안으로 들어왔습니다. 앞으로는 여기 것만 고치세요.
(이전 파일들은 `Downloads/_pcon_backup/`에 보관돼 있습니다.)

`.DS_Store` 파일은 GitHub에 올리지 마세요 — 맥이 만드는 숨김 파일이라 필요 없습니다.

## 🚀 배포하기
1. GitHub 저장소(permaculture-app)에 이 폴더의 파일을 전부 업로드
   - "Add file → Upload files" 에 폴더째 드래그 → "Commit changes"
2. Netlify가 30초 안에 자동 배포
3. 폰에서 앱을 껐다 켜면 새 버전 반영

## ✏️ 콘텐츠 수정하기 (가장 자주 할 일)
`data/content.js` 파일만 열어서 수정하면 됩니다.

- **영상 추가**: VIDEOS 목록에 한 덩어리 복사해서 붙여넣고 videoId만 교체
  - videoId = 유튜브 주소 v= 뒤의 11글자
- **농가 추가**: FARMS 목록에 추가 (region은 파일 안 안내 참고)
- **이벤트 추가**: EVENTS 목록에 추가 (date는 "2026-07-04" 형식)

수정 → GitHub 업로드 → 자동 배포. 끝!

## ⚠️ 보안 주의
firebase_adminsdk...json (비밀키 파일)은 절대 GitHub에 올리지 마세요.
이 앱은 Firebase 없이 동작하므로 그 키는 필요 없습니다.
Google Cloud 콘솔에서 해당 키를 폐기하는 것을 권장합니다.


## 📄 약관·개인정보처리방침 주소

배포하면 아래 주소가 생깁니다. **스토어 제출 폼에 넣을 URL입니다.**

```
https://<내-넷리파이-주소>/privacy.html    ← 개인정보처리방침
https://<내-넷리파이-주소>/terms.html      ← 이용약관
```

두 문서에 적힌 연락처는 `kdw1350@naver.com`이고, 앱 설정 화면의 문의처와 같습니다.
**셋 중 하나라도 바꾸면 나머지도 같이 바꿔야 합니다** — 심사에서 대조합니다.

시행일은 2026년 9월 30일로 적혀 있습니다. 출시가 미뤄지면 두 문서의 날짜를 함께 수정하세요.

## ⚙️ 앱 안에서 바꿀 수 있는 값

`index.html`에서 `CONTACT_EMAIL`로 검색하면 나옵니다.

```js
var APP_VERSION   = "1.0.0";              // 업데이트할 때마다 올리기
var CONTACT_EMAIL = "kdw1350@naver.com";  // 약관 제9조의 신고 접수처와 동일해야 함
```
