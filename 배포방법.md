# 퍼머컬처ON — 배포 방법

## 이 폴더가 유일한 원본입니다

`~/Downloads/permaculture-app/` — GitHub 저장소와 직접 연결된 폴더입니다.
여기서 고친 것이 바로 GitHub → Netlify 로 이어집니다.

> 예전 `permaculture-on 3` 폴더는 이제 쓰지 않습니다.
> 그쪽을 고쳐도 배포되지 않습니다.

## 배포하기 — Push 버튼 한 번

1. **GitHub Desktop** 을 엽니다
2. 왼쪽 위에서 `permaculture-app` 저장소를 고릅니다
3. 오른쪽 위 **Push origin** 을 누릅니다
4. Netlify 가 30초 안에 자동 배포합니다
5. 폰에서 앱을 완전히 껐다 켜면 새 버전이 반영됩니다

파일을 끌어다 올리는 일은 더 이상 하지 않습니다.
Claude 가 파일 수정과 커밋까지 해두면, 남는 건 Push 뿐입니다.

### 처음 한 번만 — GitHub Desktop 에 등록
`File → Add Local Repository…` → 이 폴더(`permaculture-app`) 선택

## 폴더 구성
```
permaculture-app/
├── index.html        앱 화면 전체
├── app.js            앱 동작
├── config.js         Supabase 주소·공개키   ⚠️ 비밀키는 절대 넣지 마세요
├── privacy.html      개인정보처리방침 (스토어 제출 필수)
├── terms.html        이용약관 (스토어 제출 필수)
├── data/
│   ├── api.js        서버 통신 (게시판·텃밭·신고·차단)
│   ├── gardenkit.js  텃밭 편집기·작물 그림
│   ├── plantdata.js  작물·동반작물 데이터
│   └── content.js    영상·농가·이벤트  ← 자주 고치는 파일
├── manifest.json     앱 아이콘·이름
├── sw.js             오프라인 동작   ⚠️ 고칠 때마다 VERSION 숫자 올리기
├── icons/  textures/  vendor/
```

## 콘텐츠만 고칠 때 (가장 자주 할 일)
`data/content.js` 만 열어서 고치고 → GitHub Desktop 에서 Push. 끝.

- **영상 추가**: VIDEOS 목록에 한 덩어리 복사 → videoId 만 교체
  (videoId = 유튜브 주소 `v=` 뒤 11글자)
- **농가 추가**: FARMS 목록에 추가
- **이벤트 추가**: EVENTS 목록에 추가 (date 는 `2026-07-04` 형식)

## 배포가 반영되지 않을 때
1. GitHub 저장소 웹페이지에서 방금 커밋이 보이는지 확인
2. Netlify 대시보드에서 배포가 성공(초록)인지 확인
3. 폰에서 앱을 완전히 종료 후 재실행 — `sw.js` 의 `pcon-vNN` 숫자가
   올라가 있어야 새 버전이 받아집니다

## 보안
- `config.js` 에는 **anon(공개) 키만** 넣습니다.
  `service_role` / `secret` 키는 이 저장소가 공개라 절대 올리면 안 됩니다.
- 카카오 Client Secret 도 마찬가지입니다.
- `firebase_adminsdk...json` 같은 비밀키 파일도 올리지 마세요.

## 약관·개인정보처리방침 주소
```
https://<넷리파이-주소>/privacy.html
https://<넷리파이-주소>/terms.html
```
두 문서와 앱 설정 화면의 연락처(`kdw1350@naver.com`)는 항상 같아야 합니다 —
스토어 심사에서 대조합니다. 시행일은 2026년 9월 30일로 적혀 있습니다.
